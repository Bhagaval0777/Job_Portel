[pytest]
DJANGO_SETTINGS_MODULE = JobPortel.settings
python_files = tests.py test_*.py *_tests.py

# conftest.py
import pytest
from django.conf import settings

@pytest.fixture(autouse=True)
def use_dummy_cache(settings):
    settings.CACHES = {
        "default": {
            "BACKEND": "django.core.cache.backends.locmem.LocMemCache",
        }
    }
    # If Axes specifically uses a different cache, point it to locmem too
    settings.AXES_CACHE = 'default'
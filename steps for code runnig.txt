for email sending setup:(SMTP)
http://youtube.com/watch?v=Mezha1p_dTE

watch the vedio and make change in settings.py

EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = ' Your@gmail.com'
EMAIL_HOST_PASSWORD = 'Your Password' # password will be generated when app is created
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER




Install Docker and open the docker
On vs code take a new terminal and run this command " docker run -d --name redis -p 6379:6379 redis " for 
creating container in docker and go to docker there will a container in that there will a start/stop button in action part, 
if already started then its ok, otherwise you have start it.


Now take a new terminal in vs code , activate environment and run " celery -A JobPortel worker --pool=solo --loglevel=info " this will activate celery


take a new terminal run django project

post : http://127.0.0.1:8000/users/register/ 
{
    "user_email":"manikandadaska@gmail.com", 
    "password":"123@aAssq0", 
    "confirm_password":"123@aAssq0", 
    "role":"jobseeker"
}

post : http://127.0.0.1:8000/users/verify-email-otp/
{
    "user_email":"manikandadaska@gmail.com",
    "otp":"424368"
}

